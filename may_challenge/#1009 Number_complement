
class Solution {
public:
    int bitwiseComplement(int num) {
        if(num==0)
            return 1;
        else
        {   int x = (int)log2(num) + 1; 
    
        
    
    for (int i = 0; i < x; i++)  
       num = (num ^ (1 << i));}  
   
    return num; 
      return 0;   
    }
};
    
